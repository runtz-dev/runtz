// Optional build-time Google OAuth web client id. Prefer configuring
// GOOGLE_CLIENT_ID on the engine, which serves it at runtime via
// /api/v1/setup/status; this constant is only a fallback for custom builds.
export const DEFAULT_GOOGLE_CLIENT_ID = ""
