import type { Metadata } from "next"

import DastRoadmap from "@/components/runtz/dast-roadmap"

export const metadata: Metadata = {
  title: "DAST roadmap | runtz",
  description: "DAST is planned for the phase after runtz 1.0.0.",
}

export default DastRoadmap
